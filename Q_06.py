def Q_06(self, full_dataset):
    # Task 6: Separate the full_dataset into two parts: X and y, where X denotes the input matrix
    # containing only the input variables, and y denotes the target vector containing only the target values
    # for exactly the same number of samples in the given full_dataset (pandas Dataframe).
    # Finally, return X and y as a tuple
    #
    import pandas as pd

    X = pd.DataFrame()
    y = pd.DataFrame()

    ## YOUR CODE HERE ##


    return (X, y)